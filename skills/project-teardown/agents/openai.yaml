interface:
  display_name: "Project Teardown"
  short_description: "Audit a software product end to end"
  default_prompt: "Use $project-teardown to execute an evidence-led, read-only product teardown and produce a validated implementation-ready handoff."
